
server:
  port: 8001
spring:
  application:
    name: payment-service
  datasource:
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: org.gjt.mm.mysql.Driver
      url: jdbc:mysql://192.168.137.131:32016/db2019?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: 123456
  cloud:
    config:
      label: master
      name: config
      profile: dev
      uri: http://localhost:3344
  rabbitmq:
    host: 192.168.137.200
    port: 5672
    username: guest
    password: guest
eureka:
  instance:
    instance-id: payment8001
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30

mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.fox.springcloud.entities
management:
  endpoints:
    web:
      exposure:
        include: "*"